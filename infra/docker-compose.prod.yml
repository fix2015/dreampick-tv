name: dreampick

services:
  frontend:
    image: ghcr.io/fix2015/dreampick-tv/frontend:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:3012:80"

networks:
  default:
    driver: bridge
